#!/bin/sh

wget https://www.badips.com/get/list/ssh/1 -O /tmp/badips-ssh
CNT=$(cat /tmp/badips-ssh|wc -l)

wget https://www.badips.com/get/list/ssh/2 -O /tmp/badips-ssh
CNT=$(cat /tmp/badips-ssh|wc -l)

wget https://www.badips.com/get/list/ssh/3 -O /tmp/badips-ssh
CNT=$(cat /tmp/badips-ssh|wc -l)

wget https://www.badips.com/get/list/ssh/4 -O /tmp/badips-ssh
CNT=$(cat /tmp/badips-ssh|wc -l)

wget https://www.badips.com/get/list/ssh/5 -O /tmp/badips-ssh
CNT=$(cat /tmp/badips-ssh|wc -l)

